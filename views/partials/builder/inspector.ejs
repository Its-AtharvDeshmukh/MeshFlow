<aside class="w-[340px] border-l border-white/5 bg-[#0a0a0c]/80 backdrop-blur-2xl flex flex-col shrink-0 z-20 shadow-[-20px_0_40px_rgba(0,0,0,0.5)]">
  <div class="h-14 flex items-center px-6 border-b border-white/5 bg-white/[0.02]">
    <h3 class="text-[10px] font-bold text-zinc-400 uppercase tracking-widest flex items-center gap-2">
        <i data-lucide="sliders-horizontal" class="w-3.5 h-3.5"></i> Node Inspector
    </h3>
  </div>
  
  <div id="inspector-content" class="flex-1 overflow-y-auto custom-scrollbar">
     <!-- Empty State -->
     <div class="h-full flex flex-col items-center justify-center text-zinc-600 text-center p-8">
        <div class="w-12 h-12 rounded-full border border-white/5 bg-white/[0.02] flex items-center justify-center mb-4">
            <i data-lucide="mouse-pointer-click" class="w-5 h-5 text-zinc-500"></i>
        </div>
        <p class="text-[11px] tracking-widest uppercase font-mono font-bold text-zinc-500">Awaiting Selection</p>
        <p class="text-xs tracking-wide mt-2 text-zinc-600">Select a node in the DAG to configure OS routing parameters.</p>
     </div>
  </div>
</aside>